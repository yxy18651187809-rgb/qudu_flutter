# 儿童识字APP - 技术架构方案

## 一、技术选型原则

1. **快速迭代**：优先选择成熟技术栈，减少踩坑时间
2. **成本可控**：充分利用开源方案，降低服务器成本
3. **易于维护**：代码结构清晰，方便后续扩展
4. **跨平台**：一套代码同时支持iOS和Android

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      客户端层 (Flutter)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ 儿童端   │  │ 家长端   │  │ 管理后台 │  │ 内容CMS  │      │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      API网关层 (Nginx)                       │
│              负载均衡 / SSL / 静态资源 / 限流                 │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      服务端层 (Node.js)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ 用户服务 │  │ 内容服务 │  │ 学习服务 │  │ 支付服务 │      │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      数据层                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ MongoDB  │  │  Redis   │  │  OSS存储 │  │  CDN     │      │
│  │ 主数据库 │  │  缓存    │  │  图片/音频│  │  加速    │      │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘      │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、技术栈详解

### 3.1 客户端 - Flutter

**选择理由**：
- 一套Dart代码同时编译iOS和Android
- 热重载开发效率高
- 性能接近原生
- 丰富的UI组件库

**技术栈**：
```
Flutter 3.x + Dart
├── 状态管理：Provider / Riverpod
├── 本地存储：Hive（轻量）/ SQLite（复杂查询）
├── 网络请求：Dio
├── 图片加载：CachedNetworkImage
├── 音频播放：audioplayers
├── TTS语音：flutter_tts
└── 埋点统计：firebase_analytics
```

**项目结构**：
```
lib/
├── main.dart              # 入口
├── app.dart               # 应用配置
├── core/                  # 核心层
│   ├── constants/         # 常量
│   ├── theme/             # 主题
│   ├── utils/             # 工具类
│   └── network/           # 网络封装
├── data/                  # 数据层
│   ├── models/            # 数据模型
│   ├── repositories/      # 仓库
│   └── local/             # 本地存储
├── domain/                # 业务层
│   ├── entities/          # 实体
│   └── usecases/          # 用例
├── presentation/          # 表现层
│   ├── pages/             # 页面
│   ├── widgets/           # 组件
│   └── blocs/             # 状态管理
└── services/              # 服务
    ├── auth_service.dart
    ├── learning_service.dart
    └── audio_service.dart
```

---

### 3.2 服务端 - Node.js

**选择理由**：
- JavaScript全栈，前后端语言统一
- 丰富的npm生态
- 异步非阻塞，适合I/O密集型场景
- 开发效率高

**技术栈**：
```
Node.js + Express / NestJS
├── 数据库：MongoDB + Mongoose
├── 缓存：Redis
├── 认证：JWT + bcrypt
├── 文件存储：阿里云OSS / AWS S3
├── 支付：微信支付SDK + 支付宝SDK
├── 推送：Firebase Cloud Messaging
├── 日志：Winston
├── 监控：Prometheus + Grafana
└── 部署：Docker + PM2
```

**API设计（RESTful）**：
```
# 用户相关
POST   /api/v1/auth/register          # 注册
POST   /api/v1/auth/login             # 登录
POST   /api/v1/auth/refresh           # 刷新Token
GET    /api/v1/user/profile           # 获取用户信息
PUT    /api/v1/user/profile           # 更新用户信息

# 儿童档案
GET    /api/v1/children               # 获取儿童列表
POST   /api/v1/children               # 创建儿童档案
GET    /api/v1/children/:id           # 获取儿童详情
PUT    /api/v1/children/:id           # 更新儿童档案

# 识字测评
POST   /api/v1/assessment/start       # 开始测评
POST   /api/v1/assessment/answer      # 提交答案
GET    /api/v1/assessment/result      # 获取测评结果

# 绘本内容
GET    /api/v1/books                  # 绘本列表（支持筛选）
GET    /api/v1/books/:id             # 绘本详情
GET    /api/v1/books/recommended     # 智能推荐

# 学习记录
POST   /api/v1/learning/read          # 阅读记录
POST   /api/v1/learning/word         # 识字记录
GET    /api/v1/learning/progress      # 学习进度
GET    /api/v1/learning/report        # 学习报告

# 支付
POST   /api/v1/payment/create        # 创建订单
POST   /api/v1/payment/notify        # 支付回调
GET    /api/v1/payment/orders        # 订单列表
```

---

### 3.3 数据库设计

#### MongoDB 集合结构

```javascript
// 用户表 users
{
  _id: ObjectId,
  phone: String,           // 手机号
  password: String,        // 加密密码
  avatar: String,          // 头像URL
  createdAt: Date,
  updatedAt: Date
}

// 儿童档案表 children
{
  _id: ObjectId,
  userId: ObjectId,        // 关联用户
  name: String,            // 昵称
  avatar: String,          // 头像
  birthDate: Date,         // 出生日期
  grade: Number,           // 年级（0=学前）
  currentLevel: Number,    // 当前等级 L1-L5
  knownWords: [String],    // 已认识汉字数组
  streakDays: Number,      // 连续学习天数
  createdAt: Date
}

// 汉字表 characters
{
  _id: ObjectId,
  character: String,       // 汉字
  pinyin: String,          // 拼音
  strokeCount: Number,     // 笔画数
  level: Number,           // 难度等级
  grade: Number,           // 对应年级
  audioUrl: String,        // 发音音频
  examples: [{            // 例词
    word: String,
    meaning: String
  }]
}

// 绘本表 books
{
  _id: ObjectId,
  title: String,           // 标题
  cover: String,           // 封面图
  level: Number,           // 难度等级 L1-L5
  theme: String,           // 主题分类
  newWords: [String],      // 生字列表
  content: [{              // 内容页
    image: String,         // 插图
    text: String,          // 文字
    wordPositions: [{     // 生字位置
      word: String,
      start: Number,
      end: Number
    }]
  }],
  isFree: Boolean,         // 是否免费
  price: Number,           // 价格（分）
  readCount: Number        // 阅读次数
}

// 学习记录表 learning_records
{
  _id: ObjectId,
  childId: ObjectId,       // 关联儿童
  bookId: ObjectId,        // 关联绘本
  type: String,            // 类型: read/learn/review
  duration: Number,        // 学习时长（秒）
  newWordsLearned: [String],
  createdAt: Date
}

// 订单表 orders
{
  _id: ObjectId,
  userId: ObjectId,
  childId: ObjectId,
  type: String,            // vip / book
  amount: Number,          // 金额（分）
  status: String,          // pending / paid / cancelled
  payChannel: String,      // wechat / alipay
  createdAt: Date
}
```

---

### 3.4 推荐算法（MVP简化版）

```javascript
// 基于识字量的简单推荐算法
function recommendBooks(childId) {
  const child = db.children.findById(childId);
  const knownWords = child.knownWords;
  
  // 计算每本书的新字数量
  const books = db.books.find({ level: child.currentLevel });
  
  const scoredBooks = books.map(book => {
    const newWords = book.newWords.filter(w => !knownWords.includes(w));
    const newWordCount = newWords.length;
    
    // 理想新字数量：3-8个
    let score = 100;
    if (newWordCount < 3) score -= (3 - newWordCount) * 10;
    if (newWordCount > 8) score -= (newWordCount - 8) * 15;
    
    // 优先未读过的
    const isRead = db.learning_records.exists({ childId, bookId: book._id });
    if (!isRead) score += 20;
    
    return { book, score, newWordCount };
  });
  
  // 按分数排序，返回前10
  return scoredBooks.sort((a, b) => b.score - a.score).slice(0, 10);
}
```

---

## 四、云服务选型

### 4.1 推荐方案（成本优先）

| 服务 | 推荐方案 | 月成本预估 |
|------|----------|-----------|
| 服务器 | 阿里云ECS 2核4G | ¥100-200 |
| 数据库 | MongoDB Atlas免费版 | ¥0 |
| 缓存 | Redis Labs免费版 | ¥0 |
| 存储 | 阿里云OSS | ¥50-100 |
| CDN | 阿里云CDN | ¥50 |
| 域名 | 阿里云域名 | ¥50/年 |
| SSL证书 | Let's Encrypt免费 | ¥0 |
| **合计** | | **¥200-350/月** |

### 4.2 部署架构

```
生产环境
├── 阿里云ECS (2核4G)
│   ├── Docker
│   │   ├── Node.js API服务
│   │   └── Nginx反向代理
│   └── PM2进程管理
├── MongoDB Atlas (云端托管)
├── Redis Labs (云端托管)
└── 阿里云OSS (静态资源)

开发环境
└── 本地Docker Compose
    ├── Node.js服务
    ├── MongoDB
    └── Redis
```

---

## 五、开发工具链

| 类别 | 工具 | 用途 |
|------|------|------|
| 代码编辑 | VS Code + Flutter插件 | 跨平台开发 |
| API测试 | Postman / Insomnia | 接口调试 |
| 数据库 | MongoDB Compass | 数据管理 |
| 设计协作 | Figma | UI设计 |
| 项目管理 | Notion / 飞书文档 | 需求文档 |
| 版本控制 | Git + GitHub | 代码管理 |
| CI/CD | GitHub Actions | 自动化部署 |

---

## 六、安全与合规

### 6.1 儿童隐私保护（COPPA/国内法规）
- 收集家长同意书
- 最小化数据收集
- 禁止第三方广告追踪
- 数据加密存储
- 支持数据导出和删除

### 6.2 安全措施
- HTTPS全站加密
- JWT Token过期机制
- API限流防刷
- SQL注入防护（使用ORM）
- XSS防护
- 敏感数据加密

---

## 七、技术风险与应对

| 风险 | 影响 | 应对策略 |
|------|------|----------|
| Flutter学习成本 | 中 | 我负责核心架构，你可以边做边学 |
| TTS语音质量 | 中 | 使用讯飞/百度TTS API |
| 内容审核 | 高 | 绘本内容人工审核后再上架 |
| 服务器成本激增 | 低 | 初期使用云服务免费额度 |
| 第三方服务依赖 | 中 | 核心数据自建，非核心用SaaS |

---

## 八、MVP技术排期（2个月）

| 周次 | 前端 | 后端 | 内容 |
|------|------|------|------|
| W1 | 环境搭建、UI框架 | 架构搭建、数据库设计 | 准备20本绘本文案 |
| W2 | 登录/注册页面 | 用户API、认证 | 汉字库整理（500字） |
| W3 | 儿童档案页面 | 儿童档案API | 绘本配图（插画） |
| W4 | 识字测评页面 | 测评API、算法 | 音频录制 |
| W5 | 绘本阅读页面 | 绘本API | 内容审核 |
| W6 | 生字本、个人中心 | 学习记录API | 内测准备 |
| W7 | 联调测试 | 性能优化 | 修复Bug |
| W8 | 内测发布 | 监控部署 | 收集反馈 |

---

*文档版本：v1.0*  
*更新日期：2026-04-20*
