# 全链路联调计划

> **创建时间**：2026年4月22日
> **负责人**：前端负责人 + 后端负责人
> **协调**：team-lead

---

## 一、联调目标

完成字趣阅读核心链路的端到端验证：
```
用户登录 → 创建儿童档案 → 选择绘本 → 阅读绘本 → 识字测评 → 查看学习报告
```

---

## 二、前置条件

| 条件 | 状态 | 说明 |
|------|------|------|
| 后端服务运行中 | ⬜ 待确认 | 需要后端负责人启动 npm run dev |
| 前端API对接完成 | ⬜ 待确认 | 需要前端负责人对接localhost:3000 |
| 数据库数据就绪 | ✅ 已完成 | 52字L1核心字已seed |
| 绘本内容就绪 | ✅ 已完成 | 3本绘本文案已就绪 |

---

## 三、联调接口清单

### 3.1 认证模块

| 接口 | 方法 | 路径 | 测试账号 |
|------|------|------|----------|
| 发送验证码 | POST | /api/v1/auth/sms/send | 13800138000 |
| 登录 | POST | /api/v1/auth/login | 验证码：123456 |
| 刷新Token | POST | /api/v1/auth/refresh | refreshToken |

### 3.2 儿童档案模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取儿童列表 | GET | /api/v1/children | |
| 创建儿童 | POST | /api/v1/children | |
| 更新儿童 | PUT | /api/v1/children/:id | |
| 删除儿童 | DELETE | /api/v1/children/:id | |

### 3.3 绘本模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 绘本列表 | GET | /api/v1/storybooks | 可选认证 |
| 绘本详情 | GET | /api/v1/storybooks/:id | 可选认证 |
| 绘本推荐 | GET | /api/v1/storybooks/recommended | 需认证 |

### 3.4 测评模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 开始测评 | POST | /api/v1/assessment/start | |
| 提交测评 | POST | /api/v1/assessment/submit | |
| 获取结果 | GET | /api/v1/assessment/result/:sessionId | |

### 3.5 学习记录模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 记录学习 | POST | /api/v1/learning/record | |
| 学习历史 | GET | /api/v1/learning/history | |

---

## 四、联调步骤

### Step 1：后端启动（后端负责人）
```bash
cd 03-后端/qudu-api
npm install  # 如需
npm run dev
# 服务应运行在 localhost:3000
```

### Step 2：Swagger验证（全员）
- 访问 http://localhost:3000/api-docs
- 验证所有接口可访问

### Step 3：认证流程（前端）
1. POST /api/v1/auth/sms/send → body: { "phone": "13800138000" }
2. POST /api/v1/auth/login → body: { "phone": "13800138000", "smsCode": "123456" }
3. 记录返回的 accessToken

### Step 4：儿童档案（前端）
1. GET /api/v1/children → Header: Authorization: Bearer <token>
2. POST /api/v1/children → 创建测试儿童
3. PUT /api/v1/children/:id → 更新儿童信息

### Step 5：绘本流程（前端）
1. GET /api/v1/storybooks → 绘本列表（无需认证）
2. GET /api/v1/storybooks/:id → 绘本详情
3. GET /api/v1/storybooks/recommended → 个性化推荐（需认证）

### Step 6：测评流程（前端）
1. POST /api/v1/assessment/start → 开始测评
2. POST /api/v1/assessment/submit → 提交答案
3. GET /api/v1/assessment/result/:sessionId → 获取结果

### Step 7：学习记录（前端）
1. POST /api/v1/learning/record → 记录学习
2. GET /api/v1/learning/history → 查看历史

---

## 五、问题记录

| 序号 | 问题描述 | 影响 | 负责人 | 状态 |
|------|----------|------|--------|------|
| | | | | |

---

## 六、联调时间安排

| 时间 | 任务 | 负责人 |
|------|------|--------|
| 14:00-14:30 | PRD评审 | 全员 |
| 14:30-15:00 | 后端启动 + Swagger验证 | 后端负责人 |
| 15:00-16:00 | 认证+儿童档案联调 | 前端+后端 |
| 16:00-17:00 | 绘本+测评联调 | 前端+后端 |
| 17:00-18:00 | 问题修复+总结 | 前端+后端 |

---

## 七、验收标准

- [ ] 所有20个API接口可正常调用
- [ ] 认证流程端到端走通
- [ ] 儿童档案CRUD正常
- [ ] 绘本列表+详情+推荐正常
- [ ] 测评开始→提交→结果正常
- [ ] Token刷新机制正常
- [ ] 无严重bug阻塞开发
